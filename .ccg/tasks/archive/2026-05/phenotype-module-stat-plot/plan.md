# Plan

1. Add `haplokit/_phenotype.py` for parsing, joining, filtering, ANOVA, pairwise tests, and TSV writing.
2. Add `haplokit/_phenotype_plot.py` for boxplot rendering with optional significance brackets.
3. Re-export phenotype helpers from `haplokit.plot`.
4. Add `phenotype` / `pheno` CLI subcommands in `haplokit/cli.py`.
5. Add focused parser, stats, CLI, and plotting tests under `tests/python`.
6. Update README with phenotype usage and dependency notes.
7. Run targeted tests, then archive the CCG task.

